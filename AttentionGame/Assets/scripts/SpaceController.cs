using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class SpaceController : MonoBehaviour
{
    //public Transform objectToMove;
    //public static Vector3 startPosition = new Vector3((float)-0.5625193, (float)0.66, (float)-0.06437713);
    //public static Vector3 endPosition = new Vector3((float)-0.5625193, (float)1.53, (float)-0.06437713);
    //public bool space = false;
    //public float duration = (float)1.0f;
    //private float startTime;

    void Start()
    {
        //startTime = Time.time;
    }

    void Update()
    {
        //    float fraction = (Time.time - startTime) / duration;

        //    if (Input.GetKeyDown(KeyCode.Space) && space == false)
        //    {
        //        // Add your action here
        //        objectToMove.transform.position = Vector3.Lerp(startPosition, endPosition, fraction);
        //        space = true;
        //    }
        //    else if (Input.GetKeyDown(KeyCode.Space) && space == true)
        //    {

        //        objectToMove.transform.position = Vector3.Lerp(endPosition, startPosition, fraction);
        //        space = false;
        //    }


    }



}